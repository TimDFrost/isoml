---
# cPanel Git deploy — runs on "Pull or Deploy" in Git Version Control
# DEPLOYPATH must match your account (public_html for isoml.com)
deployment:
  tasks:
    - export DEPLOYPATH=/home/apj6rcmrygbr/public_html/
    - /bin/cp index.html .htaccess $DEPLOYPATH
    - /bin/cp -R css js pages simulator images $DEPLOYPATH
